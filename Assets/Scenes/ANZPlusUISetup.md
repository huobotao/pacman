# ANZ Plus iPhone UI 空壳使用方式

1. 在 Unity 中新建一个空场景（或打开已有场景）。
2. 创建一个空物体 `ANZPlusUIRoot`。
3. 给它挂载脚本 `AnzPlusMockUI`（路径：`Assets/Scripts/AnzPlusMockUI.cs`）。
4. 进入 Play 模式，即可看到 ANZ Plus 风格的 iPhone UI 空壳页面。

> 说明：
> - 这是纯 UI 骨架（静态展示），未接入任何真实银行接口。
> - 文案、金额和配色都可在脚本里直接调整。
